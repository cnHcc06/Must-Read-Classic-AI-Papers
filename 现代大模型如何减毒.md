<h1 align="center">现代大语言模型如何“减毒”？</h1>
<p align="center">部分回答由 GLM 5.2 思考模式给出，请谨慎鉴别</p>

针对原文的 Figure 5(c)，作者提到：
> InstructGPT models generate less toxic outputs than those from GPT-3
according to the Perspective API. This advantage disappears when the respectful prompt is removed
(“no prompt”)

大家如果仔细观察Figure 5(c)，就会发现：如果没有要求尊重的提示词，PPO减毒效果并不好，甚至退回到和 GPT-3 差不多的水平。
这表明 InstructGPT 学到的是一种“条件反射”：它把“不产生毒性”看作是用户下达的一个指令任务。如果用户没下这个指令，它就默认恢复到预训练数据的原始分布（互联网数据本身就包含大量毒性）  

而且PPO的toxicity比基线SFT还提升了，**说明RLHF策略对“减毒”这个任务的效果不是很稳定。**

但一旦模型被摆到明面上给公众使用，这就是生死攸关的大事，那么现代的大模型是如何解决这个问题的呢？

**现在的大模型（如 Llama 2/3, GPT-4, Claude 等）主要通过以下几种方式彻底解决了这个问题：**
### 1. System Prompt（系统提示词）的固化
这是最直接的解决方案。在产品层面，现在的模型几乎不会以“空白状态”面对用户。
*   **做法**：开发人员会在用户输入之前，在后台预设一段不可见的、始终生效的 **System Prompt**。这段话类似于：“你是一个乐于助人、诚实且无害的助手。无论用户问什么，你都不要生成暴力、歧视或色情内容。”
*   **效果**：相当于 Figure 5(c) 中的“Respectful Prompt”被**永久开启**了。用户看不见，但模型一直在遵循这个指令。
### 2. 将“拒绝”内化为默认行为
InstructGPT 的时代，安全对齐更多是教会模型“听话”。现在的模型训练则更强调将安全原则内化为模型的**价值观**。
*   **做法**：
    *   **SFT 阶段**：在微调数据中混入大量的“拒绝回答”案例。不仅仅是教模型怎么回答好问题，更是教模型遇到有害问题（哪怕用户没有要求要礼貌）时直接拒绝。
    *   **RLHF 阶段**：奖励模型会被训练为：即使没有“要礼貌”的提示词，只要输出有毒，就给极低的奖励。
*   **效果**：模型学会了“安全”是底线，而不是可选的附加项。
### 3. 对抗性训练 adversarial data collection (Dinan et al., 2019)
现在的模型训练会专门针对“隐藏攻击”进行强化。
*   **做法**：红队会尝试各种手段诱导模型生成毒性（例如角色扮演、假设性场景）。如果模型在这种情况下输出了毒性，就会受到惩罚。
*   **效果**：即使没有明确的“Be respectful”指令，模型也能识别出某些请求本身是有害的，从而主动拒绝。
### 4. 宪法 AI (Constitutional AI / RLAIF)
这是 Anthropic (Claude) 提出的方法，现在也被广泛借鉴。
*   **做法**：给模型一套“宪法”（原则列表），让模型根据这些原则**自我反思**和**修正**自己的回答。这种修正过程不依赖于人类在每一条数据上标注“是否 toxic”，而是依赖于原则。
*   **效果**：这使得模型在没有具体指令的情况下，也能依据原则输出无害内容。
### 5. 输出层的“护栏”
最后兜底的一招。
*   **做法**：在模型输出内容给用户之前，中间还有一个轻量级的分类器或审核模型。
*   **效果**：如果前面的模型不幸输出了有毒内容（比如被绕过了），这层“护栏”会直接拦截并提示“内容无法显示”。这是工程上的强制保障。
**总结**：
现在的模型通过**“永远开启的系统提示词” + “内化的安全拒绝训练”**，已经将“无害”从一种“需要用户要求的指令”变成了模型的“出厂设置”，因此不再会出现 InstructGPT 那种“不说请客气就开始骂人”的情况。
