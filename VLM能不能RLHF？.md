<h1 align="center">VLM能不能RLHF？</h1>

一路走来的朋友看到 Instruction Finetuning ，或许立马就想到了同期LLM的有名工作**RLHF** 。

OpenAI团队先用人工数据对LLM初步微调（SFT），然后人工对LLM的输出进行排序打分，以此训练打分模型RM，再用打分模型作为环境强化学习。

那么这条 pipeline 能否直接复刻到多模态领域呢？

从框架上看，RLHF天然适用于多模态场景，**本文的LLaVA就突破了第一步SFT**。

| 阶段 | 纯文本LLM | 多模态LMM（可迁移版本） |  
|------|-----------|----------------------|
| **SFT** | 在高质量指令-回答对上微调 | ✅ LLaVA做的就是这一步：视觉指令微调 |
| **RM训练** | 收集人类对纯文本回答的偏好排序，训练奖励模型 | ✅ 收集人类对「图像+指令→回答」的偏好排序，训练多模态奖励模型 |
| **PPO优化** | 用RM作为奖励信号，优化LLM的生成策略 | ✅ 视觉编码器冻结，对LLM部分用PPO优化 |

那为什么不把三步全做完呢？这是因为在技术细节上多模态带来了很多新的难题需要突破。

<h2 align="center">1. SFT的困境</h2>

### "less well-defined"给人工方法带来的挑战

> when it comes to **multimodal instruction following data**, the available amount is limited, partially because the process for creating such data is **time-consuming** and **less well-defined** when human crowd-scouring is considered.

论文在Section 3开头就直指设计动机——制作多模态的指令遵循数据既**费力**、又在任务定义层面上有**模糊性和非标准化困境**。

这主要是因为LLM和VLM的SFT目标存在本质区别，LLM微调的目标是让模型**遵循用户指令、减少幻觉和有害偏见的内容** [2022 RLHF] ，而VLM主要的目标则是让模型**遵循用户指令，准确回答关于图片的各种问题**，这个目标有很大的模糊性和非标准性。因为关于一张图片，有无数个方面可以问：物体类别、颜色、数量、空间关系、情感氛围、背后的故事、构图美学，而回答也有很大的主观性和开放性。

由于对人类来说，这个任务是“less well-defined”的（标准潜藏在人类的常识和意图理解中，难以形式化），所以传统众包方法困难重重。

论文的解决方案则是用GPT-4来“定义”答案，由于LLM的指令微调已经比较成熟，还有Few-Shot Learning能力，稍作调整，就能借用它的上下文学习能力来批量生成多模态示例。

### 纯文本AI生成——信息压缩损失的瓶颈

LLaVA的操作流程是选用 text-only 的 GPT-4，将图像转换为Captions（描述文本） + Bounding Boxes（边界框坐标文本），然后把这些纯文本喂给GPT-4，让它生成多轮对话、复杂推理和详细描述。

但是这么做有一个很明显的问题：**GPT-4在回答时，只能得到关于图片的 Captions 和 Bounding Boxes，那如果这两种表征不足以支撑GPT-4回答一些细节性或复杂的问题怎么办？**

下面就是一个非穷尽的列表，简要展示Caption+BBoxes压缩会漏掉哪些关键信息：

| 视觉信息类型 | 能否被Caption捕捉？ | 能否被BBox捕捉？ | 示例问题 |
|-------------|-------------------|-----------------|---------|
| **物体精确颜色/纹理** | 部分（如"a red car"），但无法描述渐变、斑纹等复杂纹理 | ❌ | "这辆车的引擎盖上有几道划痕？" |
| **空间关系的精细几何** | 粗略（"next to", "on top of"），但无法描述精确距离和角度 | 部分（只有矩形框，无法描述旋转、遮挡、形状） | "杯子在盘子左边还是右边3厘米处？" |
| **物体计数（密集场景）** | ❌ 通常只说"a group of"或给粗略数字 | ❌ 框会严重重叠 | "这张照片里有几只斑马？"（有15只） |
| **视觉文本/OCR** | ❌ 除非恰好被描述 | ❌ | "这个店招牌上写的是什么？" |
| **非显著视觉细节** | ❌ Caption倾向于描述显著内容 | ❌ 小物体不会被标注框 | "画面左下角那个模糊的人手里拿着什么？" |
| **全局视觉属性** | 部分（光照、氛围） | ❌ | "这张照片是顺光还是逆光拍的？" |
| **视觉类比/风格** | ❌ 难以用文字精确传达 | ❌ | "这幅画的风格更像莫奈还是梵高？" |

这个问题有一些解决路径：**用更密集的Caption，采用更高分辨率的视觉表征，让GPT-4在感到困难时调用视觉工具等**

LLaVA原文就采用了更细致的Caption来弥补，但其实问题并没有想象中的严重，

- 首先：**GPT-4本身具有强大的常识推理能力**，可以根据不完整的信息进行合理推断。
- 在训练和推理阶段，LLaVA可以直接接收自己视觉编码器（ViT-L/14）的嵌入。**即使数据集的回答只基于 Captions + BBoxes，训练和推理中模型可能自发地学会利用视觉 token 中的额外信息来生成更准确的回答**（LLM特有的超越标注现象）。

可以见得，**LLM的推理能力和学习能力非常强**，还倾向展示自己的能力。这使得它在架构设计中就像万金油一样，是可以很灵活的。

### 多模态AI生成？

在Section 5.1 的实验中，论文引用了GPT-4V的回答来对比，那为什么不能直接用多模态GPT-4来生成呢？经过调查，当时虽然引用了GPT-4V，但是那大概是论文中附带的，API接口并没有开发。  

不过很快GPT-4V也就开放了，虽然4V没有详细说明训练过程，但按照OpenAI的套路，多半又是靠它的“智能工人”团队完成赛博垦荒。

<h2 align="center">2. RM训练的困境</h2>

### RM奖励模型的幻觉问题

跨过SFT的大山，想要训练好一个奖励模型也不容易。因为相比纯文本，多模态RLHF还会面临独特的挑战，其中最棘手的要数**幻觉的奖励建模问题**：

虑以下场景：

> 图像：一只猫坐在沙发上  
> 模型回答A："一只猫坐在沙发上" ✅  
> 模型回答B："一只橘猫舒适地蜷缩在皮质沙发上，阳光从窗户洒进来" ❌（幻觉：图中没有阳光和窗户）

如果奖励模型本身也是一个text-only模型，它在某些情况下可能无法准确判断回答B中的细节是否确实存在于图像中。这种 **奖励信号的不准确** 会导致RLHF优化走向错误的方向——模型学会了生成听起来更"丰富"但实际上是幻觉的内容。

### 另辟蹊径

想要在多模态上走完强化学习的流程，或许也要像SFT那样重构技术范式。

例如LLaVA原文就已经体现了Reward Model思想的萌芽：

- **GPT-4作为评估者的设计**（第5.1节）：研究团队制作的两个 Benchmarks ( *COCO* 和 *In-the-Wild* )中，就是用GPT-4从“helpfulness、relevance、accuracy、detail”四个维度给出评价，并综合打分，这正是**Reward Model的一个雏形**——只不过它还没有被闭环地用于强化学习优化。
- **"GPT-4 as judge"的模型集成**（第5.2节）：在ScienceQA上，用GPT-4判断LLaVA和text-only GPT-4的答案哪个更正确——这本质上是一次类似RLHF人工排序的**偏好选择**。

这些设计为后续的完整RLHF流水线铺平了道路。可以说，**LLaVA在方法论上已经为RLHF的介入预留了接口**，只要把GPT-4换成多模态的VLM或人类，这个管线就能立马落地。

<h2 align="center">总结</h2>

事到如今，这个方向已经不是停留在理论阶段了，已经有很多工作专门研究 RLHF for VLMs

- LLaVA-RLHF（后来有专门的论文如 Aligning Large Multimodal Models with Factually Augmented RLHF）：直接在LLaVA架构上引入RLHF，使用人类标注的视觉偏好数据训练奖励模型，然后用PPO优化，在减少幻觉方面取得了显著成效。
- RLHF-V：通过细粒度标注的**幻觉纠正数据**来训练奖励模型。
- mPLUG-Owl2 等模型也在训练流程中加入了偏好对齐的阶段。

| 维度 | 是否可迁移 | 关键挑战 |
|------|-----------|---------|
| SFT阶段 | ✅ LLaVA已完成 | 已攻克 |
| RM训练 | ✅ 框架可迁移 | 多模态偏好数据收集昂贵；奖励模型需处理视觉幻觉 |
| PPO优化 | ✅ 架构兼容 | 需要高效的视觉-语言联合生成；奖励信号的方差控制 |
| 整体闭环 | ✅ 已有多项工作验证 | 幻觉问题是多模态RLHF相比纯文本的最大增量挑战 |

总之，多模态VLMs不仅能应用RLHF，而且这种迁移既是自然的、也是必要的。LLaVA打开的大门，后续的LLaVA-RLHF等工作正在将其推向下一个阶段。